import { BlankDataPipe } from './blank-data.pipe';

describe('BlankDataPipe', () => {
  it('create an instance', () => {
    const pipe = new BlankDataPipe();
    expect(pipe).toBeTruthy();
  });
});
